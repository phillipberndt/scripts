#compdef pydoce
#
# Completion file for pydoce
#

_pydoce() {
	python -c 'import jedi; print "\n".join([x.full_name + x.complete for x in jedi.Interpreter("import '${words[2]}'").complete()])' | while read name; do
		compadd -q -S "." $name
	done
}
_pydoce "$@"
